import BlNav from '@components/public/nav';
import React, { SFC } from 'react';

const Channel: SFC<{}> = () => {
  return (
    <div>
      <BlNav />
      channel
    </div>
  );
};

export default Channel;
