type Partition {
  tid: ID!
  typename: String!
  subPartition: [Partition!]!
}

type Video {
  vid: ID!
  author: String!
  duration: String!
  pic: String!
  play: Int!
  title: String!
  description: String!
  review: Int!
}

type Tag {
  tid: ID!
  name: String!
  cover: String
  ctime: Int
  type: Int
}

type Query {
  partitionList(tid: ID): [Partition!]!
  rankingVideos(rid: ID, day: Int, offset: Int, limit: Int): [Video!]!
  videoTags(aid: ID!): [Tag!]!
}
